amcl:
  ros__parameters:
    use_sim_time: false
    alpha1: 0.2
    alpha2: 0.2
    alpha3: 0.2
    alpha4: 0.2
    base_frame_id: "base_footprint"
    global_frame_id: "map"
    odom_frame_id: "odom"
    scan_topic: /scan
    robot_model_type: "differential" # Loại xe 2 bánh vi sai
    max_particles: 2000
    min_particles: 500
    set_initial_pose: true # Cho phép tự khởi tạo vị trí (hoặc dùng RViz)